function add(number) {
	return current = current + number;
}

function subtract(number) {
	return current = current - number;
}

function multiply(number) {
	return current = current * number;
}

function divide(number) {
	return current = current / number;
}

let current = 0;
console.log(current);